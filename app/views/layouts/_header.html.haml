#main-header
  .dropdown
    %a#drop4{"data-toggle" => "dropdown", href: "#", role: "button"}
      = current_user.email
      %b.caret
    %ul#menu1.dropdown-menu.nav.nav-pills{"aria-labelledby" => "drop4", role: "menu"}
      %li{role: "presentation"}
        = link_to 'Log out', destroy_user_session_path, method: :delete
